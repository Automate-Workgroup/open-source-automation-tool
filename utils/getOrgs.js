import axios from 'axios';
let groupInfo = [];

export async function getOrgs() {
    async function getGroups() {
    console.log("Getting Orgs")    
    }
  await getGroups();
  
  groupInfo = [{group: 'test'},{group: test2}]
  
  return groupInfo
}